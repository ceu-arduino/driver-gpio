#include "out.ceu"
#include "int0.ceu"     // UNO=D2, MEGA=D21

output high/low OUT_13;

var high/low v = _;
emit INT0_GET(&v);
emit OUT(13, v);

loop do
    await INT0;
    emit INT0_GET(&v);
    emit OUT_13(v);
end
